export default function Footer({ first_name, last_name }) {
  return (
    <div>
      <h1>Let's build something durable together. {first_name.toLowerCase()}.{last_name.toLowerCase()}@gmail.com</h1>
    </div>
  )
}
